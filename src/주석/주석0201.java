package 주석;

public class 주석0201 {
/*
 * mysql 
 * Trigger
 * UPDATE외에는 대부분 INSERT는 after DELETE는 before에
 * 기존에 존재하면 old
 * 
 * procedure  = 자바의 메소드
 * 		_MSG 변수앞에 _를 붙인다
 * call = 호출해라
 * 
 * declare new_key integer; new_key라는 변수를 int로 쓸것이다
 * 
 * concat = 문자열을 합쳐준다  lpad(count(*) + 1, '6', '0')  어떤자리 6칸을 0으로 채우겠다
 * 
 * 광역시도 별로 몇개있는지 확인
 */
}
